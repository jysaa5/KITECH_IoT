package iot.java;

public class Main_01 {

	public static void main(String[] args) {
		// 삭제하기: 빈 칸 만들기: 빈 상태를 의미하는 값.
		
		
		
		//추가하기 , 현재 번호, 현재 길이
	/*	int length = 20; 
		int [] score = new int [length];
		int count = 0; // 현재 길이
		
		for (int i=0; i<100; ++i) {
		if(count>=score.length)
		{
			break;
		}
			score[count] = i*10 +6;
			++count;
	*/
		//}
		}
	}
		
		
		
		
		
		//배열은 객체다
		//score.length; // 최대 길이
	//	++count;
	//	score[count] = 10; // 1개 
	//	++count;
	//	score[count] = 10; // 2개 
	//	++count;
	//	score[count] = 10; // 3개 
		
	//	System.out.println(count+1 +"개를 저장했어요.");
		
		//배열: 연속하는 데이터(요소)들
		//요소번호: [0] ~+1 // 음수가 없음. // 경계선 확인 -> 요소 번호에 범위가 있다. 
		// 배열의 길이: 요소의 개수 - 1 (ex. 0~2 = 3개 )?
		//배열의 자료형이 같아야 한다. 
		// 기본형 배열
	//	int[] list = new int[10]; //배열형 배열이름 -> 배열 객체(클래스), new를 부친다. -> 참조형, 반드시 최대 개수 정한다. 배열은 정적 구조이다.  
	//0~9개의 요소번호가 있는것.
	//	for(int i=0; i<10; ++i) {
	//	System.out.println(list[i]); //i번 요소 
		
//	}
//	for(int i=9; i>=0; --i) {
//		list[i] = i+1;
		
//		System.out.println(list[i]);//i번 요소
	
		
		
		//TODO : 복습 - 1~25 5*5 
		// 연속하는 정수, 연속하지 않는 정수
		// 포맷 기준 2
		
	//	int data = 1;
	//	for (int row=0; row<5; row++) {
	//		for (int col=0; col<5; col++) {
	//			System.out.print(data+"");
	//		++data; // 연속하는 정수
	//		}
			
	//		System.out.println("");
	//	}
	//	}
		// 포맷 기준 1
		//int count = 0; //출력한 개수 -> 5개 출력하면 행을 내리고 이런 식으로 출력하기.
	//	for (int i=1; i<=25; ++i) {
		//	System.out.print(i+"");
	//		++count;
	//		if(count == 5) { //5의 배수
	//		System.out.println("");
	//		count = 0;
	//	}
		
		//데이터를 파악했다. -> 25가 5의 배수씩 끈어낼 수 있음.
	//	for (int i=1; i<=25; ++i) {
		//	System.out.print(i+"");
	//		if(i%5 == 0) { //5의 배수
		//	System.out.println("");
		
			
		
	
	

